"""
Plugin definition for the {{ name }} Opal plugin
"""
import os

from opal.core import plugins

from {{ name }}.urls import urlpatterns

class {{ name.title() }}Plugin(plugins.OpalPlugin):
    """
    Main entrypoint to expose this plugin to our Opal application.
    """
    urls = urlpatterns
    javascripts = {
        # Add your javascripts here!
        'opal.{{ name }}': [
            # os.path.join('js', '{{ name }}', 'app.js'),
            # os.path.join('js', '{{ name }}', 'controllers', 'larry.js'),
            # os.path.join('js', '{{ name }}', 'services', 'larry.js'),
        ]
    }

    def list_schemas(self):
        """
        Return any patient list schemas that our plugin may define.
        """
        return {}

    def roles(self, user):
        """
        Given a (Django) USER object, return any extra roles defined
        by our plugin.
        """
        return {}
